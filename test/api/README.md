# HTTP API tests

Transport tests cover the complete authenticated `/v1` surface, runtime schemas, bounded bodies, replay headers, stable problems, correlation, rate limiting, and telemetry isolation.
