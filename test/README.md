# Service Tests

Contract tests keep the frozen vocabulary and schema validation consumers aligned with the language-neutral corpus. Domain tests parameterize the same fixtures across identity, evidence, classification, availability, run transitions, recovery, and retention. Application, API, network, filesystem, race, restart, authentication, and redaction suites exercise the implemented adapters using isolated temporary data only.
