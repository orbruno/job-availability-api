# Domain Tests

Parameterized tests consume the frozen language-neutral fixture corpus for evidence, source identity, classification, availability transitions, retention, and run-state behavior. Corrections are asserted against the approved expected result while the recorded Python result remains available as migration evidence.
