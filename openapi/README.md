# OpenAPI Contract

`job-availability-v1.yaml` is the frozen OpenAPI 3.1.1 description for the local `/v1` service. The document references the canonical JSON Schema 2020-12 definitions in `../schemas/public-api-v1.schema.json`.

Contract changes require a versioned schema update, fixture coverage, a changelog entry, and an explicit design review when a public requirement or bound changes.
