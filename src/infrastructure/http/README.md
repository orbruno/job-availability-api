# Safe HTTP adapter

Outbound posting requests are bounded here. URL, DNS, redirect, connection, decompression, retry, and pacing controls are applied before content reaches the deterministic classifier.
