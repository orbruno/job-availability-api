# Service Source

Frozen contract metadata, pure deterministic domain modules, application coordination, and the API, filesystem, network, and telemetry adapters live here. Each boundary is isolated so its contract and security controls can be tested independently.
